import { redirect } from "next/navigation"
// The review viewer lives at /legal/documents — keep this stub so any
// stale bookmarks land in the right place.
export default function Stub() { redirect("/dashboard/portfolio/fund/legal/documents") }
